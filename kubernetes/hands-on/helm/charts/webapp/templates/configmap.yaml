{{- if .Values.banner.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "webapp.fullname" . }}-banner
  labels:
    {{- include "webapp.labels" . | nindent 4 }}
data:
  message: {{ .Values.banner.message | quote }}
{{- end }}
