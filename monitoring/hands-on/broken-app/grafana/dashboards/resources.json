{
  "annotations": { "list": [] },
  "editable": false,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus-main" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "percentunit" }
      },
      "gridPos": { "h": 9, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "Process CPU (cores) per service",
      "type": "timeseries",
      "targets": [
        {
          "expr": "rate(process_cpu_seconds_total{service=~\"gateway|orders|users|payments\"}[2m])",
          "legendFormat": "{{service}}",
          "refId": "A"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus-main" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "bytes" }
      },
      "gridPos": { "h": 9, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Process RSS per service",
      "type": "timeseries",
      "targets": [
        {
          "expr": "process_resident_memory_bytes{service=~\"gateway|orders|users|payments\"}",
          "legendFormat": "{{service}}",
          "refId": "A"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus-main" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "short" }
      },
      "gridPos": { "h": 9, "w": 12, "x": 0, "y": 9 },
      "id": 3,
      "title": "Orders pool: in-flight vs capacity",
      "type": "timeseries",
      "targets": [
        { "expr": "orders_inflight_requests", "legendFormat": "in-flight", "refId": "A" },
        { "expr": "orders_pool_capacity",     "legendFormat": "capacity",  "refId": "B" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus-main" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "bytes" }
      },
      "gridPos": { "h": 9, "w": 12, "x": 12, "y": 9 },
      "id": 4,
      "title": "Users service: synthetic leak gauge",
      "type": "timeseries",
      "targets": [
        { "expr": "users_leaked_bytes", "legendFormat": "leaked", "refId": "A" }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus-main" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "unit": "s" }
      },
      "gridPos": { "h": 9, "w": 24, "x": 0, "y": 18 },
      "id": 5,
      "title": "Process uptime per service (resets on restart)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "time() - process_start_time_seconds{service=~\"gateway|orders|users|payments\"}",
          "legendFormat": "{{service}}",
          "refId": "A"
        }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["broken-app", "resources"],
  "templating": { "list": [] },
  "time": { "from": "now-30m", "to": "now" },
  "timepicker": {},
  "timezone": "",
  "title": "Broken App — Resources",
  "uid": "broken-app-resources",
  "version": 1,
  "weekStart": ""
}
